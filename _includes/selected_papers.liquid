<div class="publications">
  {% assign selected_papers = site.bibliography | where: "selected", true | sort: "order" %}
  {% for paper in selected_papers %}
    {% bibliography --query @*[key={{ paper.key }}]* %}
  {% endfor %}
</div>
